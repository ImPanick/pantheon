# Roadmap verification — integrator's pass

**Adversarial check performed.** I re-verified all five `ALREADY_DONE` verdicts against `/work/pantheon` @ `fff72ec` myself; all five hold. I also re-ran the two contested ticks, the three riskiest `PREMISE_FALSE` calls, `check-wiring.py`, and five disputed counts. **No `CONFIRMED` verdict in the material lacks evidence** — every one carries a `file:line` citation or an executed measurement. Three groups declared measurement gaps honestly; those are in § Unverified, not hidden.

---

## Already done — tick these

| id | what proves it (I verified each) | trace to write on the line |
|---|---|---|
| **P0-09** | All six named renames present: `src/runtime_paths.py:29` `~/.pantheon/data`; `pantheon-ui.service`; `Pantheon.spec`; `build-macos-app.sh:56` `com.pantheon.launcher`; `static/manifest.json:2-3`; `static/sw.js:10`. `.odysseus` occurs **once tree-wide — in the task line itself** | `6/6 renamed pre-verification; .odysseus 0 hits outside .pantheon/` |
| **P2-22** | `editor/build/controls.js:358` `ge-upscale-section`, `toolbar.js:35` `{id:'upscale'}`, `ai-tools-misc.js:120` → `/api/image/upscale-local`. Section-id gap now 13 built / 13 looked up / 0 missing | `controls.js:358 + toolbar.js:35; backend = upscale-local per DECIDED` |
| **P2-23** | `static/index.html:485/486/487` — all three ids present; `rag.js:143` bail-out no longer fires; revives orphaned CSS at `style.css:2392` | `index.html:485-487; user-facing rag.js, not adm-rag*` |
| **P8-01** | `static/index.html:389/393/397/401/405` — all five inputs; read at `skills.js:1935-1944`, cleared `:1970-1972` | `wiring run 01; zero JS changed; unblocks P9-06` |
| **P9-15b** | `chatRenderer.js:2516-2546` `.ask-user-other` input + send + Enter, `:2546` appended on non-approval cards; CSS `style.css:40940-40957`; live on all 3 render paths | `extends .ask-user-card in place; withheld from tool_approval by design` |

### Ticks that do NOT hold — revert these to `- [ ]`

| id | why | proof |
|---|---|---|
| **P0-08** | `ODY_USER` identifier never renamed — only its *value* was | `docker/entrypoint.sh:29,30,45,141,146`; `:30` reads `[ -z "$ODY_USER" ] && ODY_USER=pantheon`. Same class: `_ODY_VENV_FOR_LIBS`/`_ody_nvlib`/`_ODY_LLAMA_SHIM_EOF` at `routes/cookbook_routes.py:138-141,2251` |
| **P0-14** | Second upstream identity never named — `odysseus-dev/odysseus` = **0 hits** in NOTICE, README.md, CREDITS.md, ACKNOWLEDGMENTS.md (verified by grep). D-2026-08-26-06 requires both be named | `NOTICE:23` names only `pewdiepie-archdaemon/odysseus` |

---

## Premise wrong — fix the line before anyone starts

| id | what the line says | what is actually true |
|---|---|---|
| **P1-07** | syntax palette "never wired to the theme system" | 10 `--hl-*` tokens declared, used 97×, recomputed per theme at `theme.js:270-281` and `index.html:45-56`. Real remainder = 135 loose hexes outside `:root`, ~half duplicates of `--red`/`--green` → P1-06's job |
| **P3-10** | three dead modules including RAG | RAG is **live** (P2-23 landed its markup). Two dead: `calendar/reminders.js` (114 lines, 0 importers), `tourAutoplay.js` (133 lines of *working* code — only `init()` is stubbed, not "the whole body") |
| **P13-10** | "computed and thrown away, same as P4" | Fully wired end-to-end: `chat_processor.py:311/330/347` → `chat_helpers.py:1068` → `chat_routes.py:1622` → `chat.js:3417` → `chatRenderer.js:1847` `.memory-used-pill` + detail panel. Only *confidence* is missing → collapses to a one-field extension of P13-01 |
| **P9-07** | "not one empty state exists anywhere" | ~54 sites across 20 class names; `calendar.js:827-855` is a complete one; shared helper `ui.js:833`. Cookbook clause also false (`cookbookRunning.js:2411` renders real output + diagnosis) |
| **P9-09** | "chat bubbles show provenance; nothing else does" | 4 of 6 already do (`memory.js:776`, `skills.js:208`, `gallery.js:1286/1481`, `research/panel.js:897`). Only tidy results + calendar parses lack it. "The formatter already exists" is false |
| **P9-14** | count computed and never rendered; unreported bulk loop | Rendered in 4 live bulk bars; 3 doc ops + 1 gallery op already report done/failed. The loop described is in `sessions.js:3283-3312` inside `#library-modal`, which is **unreachable** |
| **P9-15c** | vector search exists, keyword doesn't | Backwards. FTS5+LIKE ships (`session_search.py:300`). Zero chat-message embedding lane — 3 Chroma collections, none over messages. Scope grows: needs indexing + backfill |
| **P11-01** | "requires a registry of known privilege keys, which does not exist" | `DEFAULT_PRIVILEGES` **is** it — 11 keys (AST-verified), and `set_privileges` already filters on it. One-line guard at `auth_helpers.py:172` |
| **P13-03 / P13 preamble** | "`memories` is a flat table behind a vector index" | Live store is `data/memory.json` (`src/memory.py:137/277`); the SQL table has 2 non-test readers. P13-01's confidence is a JSON key + default, **not** a migration |
| **P14-01** | write it "where totals are computed in `llm_core.py`" | Totals accumulate at `chat_helpers.py:828-844` (4 callers); `llm_core` writes no total. 17-line insertion point vs a 3,731-line file |
| **P1-05** (2nd half) | the guided tour fails because of it | Tour uses `#user-bar-settings` (`slashCommands.js:3337`); the rail branch is unreachable dead code. **Current Verify passes today** — rewrite it or this gets ticked without a fix |
| **P2-19** | two functions throw on `null.innerHTML` in a silent try | Neither is registered (`inits`/`refreshAll` omit both) so neither runs; `initWebhookForm` has no `try` at all. Fix = markup **plus** registration |
| **P3-17** | same webhook example; Verify = "no bare `except: pass`" | Example refuted; **0** bare `except:` in non-test Python, so the Verify passes trivially. Real target: 199 `except Exception: pass` pairs |
| **P4-24** | resumed run "unobservable after the fact" | It reloads and replays from persisted `tool_events`. What's lost is the **live** view during the resumed stream |
| **P4-25** | "all of it is on the wire; none is kept" | Wrong both ways — 5 of 8 receipt items already persist (`chat_helpers.py:1044-1070`); 3 are neither on the wire nor kept |
| **P4-06** | failed turn renders identically to a success | Reply text carries `[Agent stopped: …]`. Identical part = metrics footer + stats popup |
| **P7-11** | no real file export | `GET /api/session/{sid}/export` already serves md/txt/json/html as attachments (`session_routes.py:804`), reachable only via `/export`. Approval-trail export is impossible — approvals are in-memory, 10-min TTL |
| **P9-04** | "highest-priority IA fix" | Consolidation already landed (`index.html:2100-2124`). Remainder = delete two dead forms (`eaf-*`, `set-email-*`) |
| **P9-05** | build calendar views + N-way compare | Both exist. The Compare-in-a-box half contradicts its own protected constraint (`compare/index.js:328-336`) |
| **P13-04** | build reinforcement + decay | Reinforcement ships (`memory.py:297-315`, `chat_processor.py:352`, "Most used" sort). Only decay/archive is open |
| **P10-08** | every new modal needs its own `ui-scale-125` line | Backwards — `style.css:181` generic rule covers all `.modal-content`; the 5 per-modal lines are exceptions |
| **P10-03** | two resize handles | Three. `#settings-sidebar-resize-handle` is already done and is the template |
| **P8-02** | those fields are "reachable from nowhere" | Reachable via the raw SKILL.md card editor (`skills.js:1034`) — a Law 15 failure, not Law 13 |
| **P8-42** | a server relying on PATH/HOME fails | Both are in `DEFAULT_INHERITED_ENV_VARS` and survive. What vanishes is PYTHONPATH/NODE_PATH/npm cache/proxy — and only when env is empty |
| **P11-02d** | the 15 files "make no auth call of their own" | 9 of 15 call `get_current_user`/`owner_filter`; `chat_routes.py:338/367` performs an admin check via `owner_is_admin_or_single_user`. The entry also ends in mis-merged text from P11-02 |
| **P0-28** | README links the root ROADMAP as "Tracker" | All three README refs point at `.pantheon/ROADMAP.md`. Collision is filename-only |
| **P12-06** | "fires on a normal multi-file drag" | Already fixed (`upload_routes.py:285-291`, #1346). Remaining defect: `3` is a constant and "concurrent" means a 10-second window |
| **P12-04** | reconcile three char budgets | Five in `document_processor.py` alone (incl. a `.log`-only 10,000 branch), seven total. `context_budget.py` already implements the wanted shape |

---

## Numbers corrected

Agent count wins where scope was stated (Law 5). Rows marked ⚠ = the original task stated **no** scope, so its figure is unreproducible rather than merely wrong.

| id | claimed | measured | scope counted |
|---|---|---|---|
| P1-01 | 508 | **521** ✔I re-ran: 521 | `var(--accent, var(--red))` literal in `static/style.css`; 4 independent methods agree. 799 bare-token sites ✔ confirmed |
| P1-08 | all 16 themes fail; 7 of 16 | **15 of 16**; **8 of 16** | WCAG 2.x relative luminance per theme against resolved send-btn bg / theme bg at 4.5:1 |
| P1-10 | 261 decls / 63 values | **259 / 64** | `static/style.css` only; 63 numeric + one `var()` |
| P1-13 | 274 / 198 / 110 | **287 / 212 / 112** | `static/style.css`, comments stripped. New: 156 of 287 already token-aware |
| P1-06 ⚠ | 517 / 68 / 420 | not reproducible | Nearest: 346 occurrences / 75 distinct non-grey hexes outside `:root` |
| P1-07 | ~120 | **150** (135 outside `:root`) | One-Dark ∪ Dracula palette, 27 values, `static/style.css` |
| P0-13 | 5 boat SVG copies | **9** (6 excl. `docs/index.html`) | wave path `M4 24Q10 20 16 24`, 4 files |
| P0-29 | 3,533 / 172 files | **3,529 / 171** | case-insensitive `cookbook`, tracked files, excl. `.pantheon/` |
| P0-03 (ticked) | 99 names / 560 refs | **103 / 574** | `PANTHEON_*`, tracked, excl. `.pantheon/` |
| P0-10 (ticked) | 19 CLI scripts | **20** | `pantheon-*` in `scripts/` minus init + repo-setup |
| README:75 | "about three hundred" | **206** | 205 bare `var(--accent)` + `style.css:6775` whose whole chain is undefined |
| P2-14 | 81 tools | **82** | executed `known_tool_names()` in-tree |
| P2-21 | three admin endpoints | **4 endpoints, 2 gated** | `skills_routes.py:1250/1287/1311/1337`; both GETs open |
| P3-03 ⚠ | 510 / 2,590 / 349 | unreproducible | classifier absent from repo; proxy = 527 of 3,303 class names |
| P3-04 | 3 duplicate keyframes | 3 names, **4 blocks** | `spin` has two extras |
| P3-06 | nine names | **9 definitions, 7 names** | body `to{transform:rotate(360deg)}` |
| P3-07 | 3,066 lines / 560-line block | **2,965 / 559** | 85 `max-width:768px` blocks, span-summed |
| P3-12 | four orphan ids | **26** | 476 ids in `index.html`; 31 unread by JS; 26 also absent from CSS |
| P4 preamble | ~50 SSE types | **39** | `type` key of dicts serialised into `data:` frames on `/api/chat` |
| P5-01 | three 300px scrollers | **2** | CSS rules pairing `max-height:300px` with `overflow-y:auto` in trace markup |
| P5-12 | six tool chips | **7** | `input-icon-btn tool-indicator` in `static/index.html` |
| P5-13 | 1,182 SVGs / 8 sizes / 9 strokes | **1,193 / 20 / 14** | `static/*.html` + `static/js/**` + `static/app.js`, excl. lib. Task's 8/9 = `index.html` alone |
| P5-14 | 827 in 10–12px | **829** | `font-size:<N>px` decls in `static/style.css` (1,222 total, 27 steps ✔) |
| P6-04 | two research-specific lines | **~16, across 7 endpoints** | case-insensitive `research` in `research/jobs.js` |
| P6-05 | 3 documented statuses | **6 written** | `db.py:818` comment vs `task_scheduler.py` writes |
| P7-01 | 60-word regex; 35 lines later | **58 alternations; 33 lines** | parsed the alternation group at `chat.js:1884` |
| P8-30 | three event catalogues | **2 enumerated + 5 hardcoded** | `task_routes.py:1035-1043`, `tool_schemas.py:583`, `task_scheduler.py:241-251` |
| P8-41 | three stale comments | **2** | multiline grep across src/routes/core/services/static/docs |
| P8-45 | 23 presets / ~400 lines | **15 presets (14 with help) / 420 lines** | ✔ I re-verified by balanced-bracket parse: 15 top-level objects, `admin.js:1793-1859`. A naive `{ name:` regex returns 23 — that is how the wrong figure was produced |
| P10-01 | 97 `outline:none` | **97 ✔** (+2 `outline:0` = 99 suppressions) | `static/style.css`, occurrence count |
| P10-05 / P1-12 | 148 keyframes | **160** (148 CSS + 12 JS-injected) | `slashCommands.js` injects 12 into `document.head` — a CSS-only guard cannot reach them |
| P11-01 | 9 privilege keys | **11** ✔ AST-verified | `DEFAULT_PRIVILEGES` in `core/auth.py:24-38` (9 bool + 1 int + 1 list) |
| P11-02 ⚠ | 84 `require_admin` / 17 priv / 58 owner_filter | **103 ✔ / 16 / 32** | ✔ I re-ran: 103. Non-test `.py`, excl. the def and imports. Six scopes tried for 84; nearest 82 |
| P11-10 | 60 `TOOL_SECTIONS` | **60 ✔** AST-verified | — |
| P12-01 | eleven byte caps | **10** | distinct `PANTHEON_*BYTES` env names in non-test `.py`; 7 in `upload_limits.py`, 1 backup, 1 TTS, 1 lazy |
| P12-03 | caps read at import | **8 of 10** | `get_chat_upload_max_bytes` re-reads per call; TTS reads at instance init |
| P13-02 | one grep hit | **6 raw, 0 relevant** | `link\|related\|edge\|graph` across `services/memory/*.py` |
| P8-22 | — | `BUILTIN_ACTIONS` **18** vs `BUILTIN_ACTION_INFO` **16** ✔ AST-verified | `run_local`, `cookbook_serve` never offered by `/meta/actions` |
| **check-wiring** | UNRESOLVED 2 | **2 ✔ literal-`getElementById`; 125 helper-aware** | ✔ I reproduced 1,351 lookups / 125 unresolved (`settings.js` 51, `admin.js` 45, `app.js` 27) using the checker's own resolution rule extended to `el()`/`_el()`. Three groups measured 96/97/125 independently; the 125 method is the one that reproduces |

---

## Superseded — close these

- **P2-10** → D-2026-08-26-06 reverses the body; work lives at **P12-06**. Leaving the "drop it" sentence on the line is a Law 10 hazard.
- **P2-25** → "Prune nothing." Remaining work is documentation only. The title still says "Prune…" — an agent that stops before the DECIDED clause will prune. `resolve_contact` is the trap.
- **P2-26** → same decision. Must-stay list omits `POST`/`DELETE /api/cookbook/state`, the two data-destruction guards no test pins.
- **P1-07** → action already done; fold the 135 loose hexes into **P1-06**.
- **P7-05** → not independently actionable (the ladder UI does not exist). Re-file as an acceptance criterion on P7-03/P7-04, citing `design/pantheon-v10.html:1721-1727`.
- **P13-10** → reduce to "add confidence to the existing trace"; depends entirely on P13-01.
- **P13-04** (reinforcement half) → done; keep only decay + archive.

---

## Blocked — cannot start today, and why

| id | blocker |
|---|---|
| P0-05 | ChromaDB `localhost:8100` unreachable; the task's own Verify requires it. Code half verified only |
| P0-17 | Repo must be public for the §13 href to resolve — D-2026-08-26-02 keeps it private until P0-14…P0-27 close |
| P0-19 | Two credits files disagree on which is authoritative; `NOTICE:41-42` points at the wrong one. Gates P0-20/24/25/26 |
| P1-06 | Scope unstated and unreproducible; adding `--ok/--warn/--danger/--info` forks a **third** colour vocabulary (`--warn` already exists at `style.css:30`) |
| P2-13 | 4 assertions pin the cap (`test_llm_core_temperature_reasoning.py:104`, `test_pr6020_rebase_regressions.py:182/201/216`); mixed fallback-chain property must survive |
| P3-03 | Classifier does not exist and must be rebuilt with the `check-wiring` scope lesson applied — **and P2-20 must land first** or the pass deletes 16 `admin-rag-*` rules P2-20 needs |
| P3-08 | Self-referential citation with no antecedent; also must follow P3-07 or 85 comments get written twice |
| P3-19 | P13-07 unstarted — the artefact to be made resilient does not exist |
| P4-21 | D-01 freezes the approval card's markup (2 CI files assert literal source strings). Emit half can land |
| P4-26 / 27 / 28 | Chain on P4-25, which chains on P4-16 |
| P5-09 | Nothing emits the taint list (`external_sources` = 3 occurrences, 0 readers). One P4-16 emit unblocks this and P7-08 together |
| P7-05 | P7-03/P7-04 — the surface does not exist |
| P7-06 / 07 / 08 | D-01 caps at compute-and-send + existing selectors. **Its revisit condition is unmeasurable here** (see Unverified) |
| P7-11 | Approval trail is not persisted (in-memory, 600s TTL) — persistence first |
| P8-09 | `_run_skill_test_once` destructively **denies** a pending approval on gate-hit; running it twice burns two approvals. Needs P8-10 |
| P8-11 | Hard-blocked on P8-10 — nothing to roll back to |
| P8-25 | No `ALTER TABLE task_runs ADD COLUMN steps` exists anywhere; writing to it raises `OperationalError` on any upgraded DB |
| P8-34 | P8-22 + P8-25 + P8-26 — no graph to draw |
| P8-45 | P2-20. Also Law 14: `settings.js:5000` already ships a working MCP form |
| P9-04 / P9-14 | Deletions need explicit **DELETE** marking under Law 1 |
| P9-12 | Undo path depends on P8-10 (predicate fix at `skills.js:1778` is independent) |
| P9-16 | Its Verify line requires reduced-motion, which `theme.js` has zero of → P1-12 |
| P10-04 | P1-09 — no contrast machinery to audit against |
| P10-05 | P1-12, which carries the same understated keyframe count |
| P10-11 | Sequenced by D-2026-08-26-02 to immediately before the public flip |
| P11-04 / 05 / 06 | P11-03 (no OIDC exists — 2 grep hits, both unrelated comments) |
| P11-09 | 2 tests actively pin the deletion (`test_upload_multifile.py:297/310`) **and** no multi-user flag exists to gate on |
| P11-10 | P2-21's missing list loader: `builtinSkills` is never assigned from any fetch; flipping the flag ships an empty section |
| P12-02 / 07 / 08 / 09 | P11-02 (roles), P2-20, P14-01, P12-04 respectively |
| P13-01 / 02 / 05 / 09 | Must land the P13-03 store correction first, or all four target the wrong store |
| P14-02 / 03 / 05 | P14-01 (+ P12-10 for the approval-outcome half) |
| P14-04 | Four open prerequisites: P4-26, P4-27, P4-28, P14-03. Most-blocked line in the tracker |

---

## Verified open and accurate — safe to implement

- **P0 (7):** 16, 20, 21, 22, 23, 25, 26
- **P1 (6):** 03, 04, 09, 11, 12, 14
- **P2 (7):** 05, 12, 14, 18, 20, 21, 24
- **P3 (6):** 02, 05, 09, 11, 15, 18
- **P4 (10):** 03, 04, 05, 07, 08, 12, 14, 16, 17, 20
- **P5 (10):** 02, 03, 04, 06, 07, 08, 10, 11, 15, 16
- **P6 (13):** 01, 02, 03, 07, 08, 10, 11, 12, 13, 14, 15, 16, 17
- **P7 (7):** 02, 03, 04, 07, 08, 09, 10
- **P8 (32):** 03, 04, 05, 07, 08, 10, 13, 14, 15, 17, 18, 19, 20, 23, 24, 26, 27, 28, 29, 31, 32, 33, 36, 37, 38, 39, 40, 43, 44, 46, 47, 48
- **P9 (9):** 01, 02, 03, 06, 08, 10, 12, 13, 16
- **P10 (5):** 01, 02, 06, 07, 09
- **P11 (2):** 03, 07
- **P12 (3):** 05, 06, 10
- **P13 (4):** 00, 01, 06, 08
- **P14 (0)**

**Total 121 of 282.** Several carry a drifted line-number citation listed above; none carries a premise-level error.

---

## Unverified

Nobody actually opened these. Saying so is the honest outcome.

- **P0-05's live-instance half** — "RAG is empty", "`pantheon_tool_index` does not exist yet". ChromaDB unreachable from the container; the task's own Verify requires that endpoint.
- **P3-03's 510 / 2,590 / 349** — the classifier that produced them is not in the repo. Unreproducible *and* stale: the wiring run deleted 1,524 lines of JS after they were measured.
- **P3-08** — premise checked by grep for pairing comments; no per-rule pairing audit performed.
- **P3-12's unnamed items** — "two elements killed by CSS" and "a handler wired to a nonexistent element" are not itemised anywhere, so they cannot be verified or ticked as written.
- **P3-19** — dependency check only; nothing to verify until P13-07 lands.
- **P4-26** — `regenerateFrom`'s send path past `chat.js:5730` unread; whether it can accept a config override is unknown.
- **P4-28** — `compare/panes.js`, `scoreboard.js`, `vote.js` unread; whether the compare layout can host a non-model diff is unknown.
- **P5-09** — its dependency (the P4-16 emit shape) was not traced; that's another group's phase.
- **P8-11, P8-33, P8-34** — absence-grep only, no design pass.
- **P8-21's token figure** — chars/4 approximation; no tokenizer in the container.
- **P9-08** — error strings sampled, not enumerated. The task has **no Verify line at all**, so under Law 9 it can never be honestly ticked.
- **P10-06, P10-10** — the sweeps themselves were not performed. P10-10's "manual pass over every surface in the mockup" is unbounded: `design/pantheon-v10.html` is not indexed by surface.
- **P14-04** — receipts *source* never opened; only the three P4 roadmap rows were checked.
- **P14-07** — `src/chroma_client.py` internal batching not audited; "0 paced" covers callers only.
- **D-01's revisit condition** — whether upstream commits have stopped landing daily **cannot be answered from this environment**. `/work/base` HEAD is the fork point (2026-08-20); today is 2026-08-27. D-01 stays binding by default.
- **122 of the 125** helper-scope dead lookups were not individually adjudicated. Treat 125 as an order-of-magnitude claim about the checker's blind spot, not an exact defect count.
- **P13-00** — an acceptance criterion, not source-checkable.

---

## What to run next

**Run the P0 licence-compliance block: P0-19 (decide) → P0-16, P0-20, P0-21, P0-22, P0-23, P0-24, P0-25, P0-26.**

Why that one:

1. **It is the only phase gating something external.** D-2026-08-26-02 keeps the repo private until these close. Everything else in the tracker can proceed in parallel behind it; nothing else can proceed *instead* of it.
2. **Accuracy is highest per task.** 7 of the 9 open rows verified clean with exact citations, and the only two corrections (P0-24's `httpx2`, P0-13's boat count) are additive. Compare P4: 18 of 28 rows needed correction, and both preamble headline numbers were wrong.
3. **Blast radius is near-zero on code.** P0-16/20/21/22/24 are file headers and additions under `licenses/`; P0-23 is one deletion of a provably-false artefact (1,468-byte, 3-glyph FontForge scratch file claiming to be GohuFont); P0-25/26 are prose. No CSS, no JS, no `CACHE_NAME` bump, no test churn, no rebuild.
4. **Dependency order is one decision then mechanical work.** P0-19's "which file is the credits file" gates P0-20, P0-24, P0-25 and P0-26 — four rows unblocked by a single call. P0-26 should follow P0-25, since the "fully permissive core" sentence at `ACKNOWLEDGMENTS.md:151` is partly propped up by the PyMuPDF scope claim P0-25 disproves.
5. **P0-09 retires for free** the moment you tick it, and two false ticks (P0-08, P0-14) are in this same block — leaving them means shipping a public repo whose AGPL §5(a) attribution is knowingly incomplete. That is the single worst outcome available in this tracker.

**Explicitly out of scope for that run:** P0-13 and P0-29. The Cookbook→Forge sweep is 3,529 occurrences across 171 files and 43 paths — the largest blast radius in the tracker, coupled to `_ROUTE_FAVICON_SHAPES` and to `ODY_` residue P0-08 should have cleared. It deserves its own session, as D-2026-08-26-06 already says.

**Second choice, if the licence work is someone else's:** **P6 (queue and plan mode).** 13 of 18 rows verified accurate, contained to `chat.js` + `app.js` + two scheduler files, no cross-phase gate, and P6-01/02/03 is a coherent user-facing bug cluster (queued messages fire into the wrong chat, vanish on reload, and silently swallow a send with attachments).

**Do not start with:** P1 (three documents carry a wrong `508` that must be corrected first, and P1-01's real scope is three files plus a `CACHE_NAME` bump, not one module); P4 (P4-01's six-template unification gates eight downstream lines); P3-03 (its measurement does not exist); P11–P14 (P14-01 unblocks five rows and its stated write location is wrong).

**Cheap housekeeping to do first — blocks nothing, takes minutes:** untick P0-08 and P0-14; tick the five done rows and fix the P8 status row (48/47/0/1); repair `ROADMAP.md:66`, whose Total row is ten concatenated snapshots and states no total; fix `FORBIDDEN.md:126` (`ODYSSEUS_MCP_ALLOWED_COMMANDS` no longer exists), its MCP disabled-tool-list claim (stores **bare** names, not qualified), and its `/find` search-overlay claim (`/find` never touches the overlay); fix `README.md:75-76` "about three hundred" → 206. Every one of these is a governing document stating something the source contradicts.